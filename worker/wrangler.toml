name = "atelier-estetico-proxy"
main = "worker.js"
compatibility_date = "2024-09-01"

# Worker free tier · 100k requests/dia
# CPU limit padrão (10ms) é suficiente, mas inpainting demora.
# O tempo de espera não conta como CPU time, apenas o processamento JS.

[observability]
enabled = true
